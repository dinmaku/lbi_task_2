export default {
  theme: {
    extend: {
      fontFamily: {
        sans: ['Inter', 'sans-serif'],
        serif: ['Georgia'],
        mono: ['Menlo'],
      },
    },
  },
  plugins: [require('tailwind-scrollbar')],
}